{
  "name": "realux-functions",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions,firestore,storage,auth",
    "test": "npm run build && node --test lib/**/*.test.js",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@noble/curves": "^2.2.0",
    "@noble/hashes": "^2.2.0",
    "@stablelib/aes": "^2.0.1",
    "@stablelib/gcm": "^2.0.1",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
